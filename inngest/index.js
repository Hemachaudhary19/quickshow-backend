import { Inngest } from "inngest";

// create inngest client
export const inngest = new Inngest({ name: "Quick Movie Show" });

// define functions (empty for now, add later)
export const functions = [];
